# Point Video Maker to the mounted USTC Cloud Drive BGM directory.
BGM_DIR=/home/ubuntu/.mounts/video-maker-bgm

# Optional proxy settings if this host needs them for other traffic.
HTTPS_PROXY=
HTTP_PROXY=
